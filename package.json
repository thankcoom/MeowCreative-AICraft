{
  "name": "blog-image-generator",
  "version": "1.0.0",
  "description": "自動化部落格圖片生成系統",
  "scripts": {
    "generate-images": "node scripts/generate_images.js",
    "install-deps": "npm install && pip3 install -r requirements.txt"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.4.0",
    "puppeteer": "^23.9.0",
    "chart.js": "^4.4.1",
    "canvas": "^2.11.2"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
