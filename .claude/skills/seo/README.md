# SEO 搜尋意圖分析工具

**版本**: 1.0.0
**建立日期**: 2025-10-24

## 📖 簡介

這個工具用於分析關鍵字的搜尋意圖，並提供針對性的內容結構建議，幫助文章更好地匹配使用者的搜尋需求，從而提升 SEO 效果。

## 🎯 功能特色

- **智能意圖識別**: 自動判斷關鍵字屬於哪種搜尋意圖
- **結構化建議**: 針對不同意圖提供標題格式、必備元素、文章結構
- **匹配度分析**: 評估現有文章與搜尋意圖的契合度
- **優化建議**: 根據分析結果提供具體改進方向
- **可配置規則**: 透過 YAML 配置自訂匹配規則

## 🔍 支援的搜尋意圖類型

| 意圖類型 | 說明 | 範例關鍵字 |
|---------|------|----------|
| **教學型** (Tutorial) | 使用者想學習如何做某事 | "如何使用 Claude Code"、"Python 量化交易教學" |
| **資訊型** (Informational) | 使用者想了解概念或定義 | "什麼是 AI Agent"、"Claude Code 介紹" |
| **比較型** (Comparison) | 使用者在多個選項間決策 | "Claude vs ChatGPT"、"Python vs JavaScript" |
| **評價型** (Review) | 使用者尋求真實使用經驗 | "Claude Code 好用嗎"、"FinMind 評測" |
| **問題解決型** (Problem Solving) | 使用者遇到具體問題 | "解決 API 錯誤"、"修復連接失敗" |
| **交易型** (Transactional) | 使用者準備購買或註冊 | "Claude Code 價格"、"免費試用" |

## 🚀 使用方式

### 1. 基本用法 - 分析關鍵字

```bash
python .claude/skills/seo/intent_analyzer.py analyze "如何使用 Claude Code"
```

**輸出**: 在終端顯示 Markdown 格式的分析報告

### 2. 產生報告檔案

```bash
python .claude/skills/seo/intent_analyzer.py analyze "Claude Code 教學" \
  --output output/session_xxx/intent_analysis.md
```

**輸出**: 分析報告儲存至指定路徑

### 3. 分析現有文章的匹配度

```bash
python .claude/skills/seo/intent_analyzer.py analyze "AI Agent 開發" \
  --article output/session_xxx/final_article.md \
  --output output/session_xxx/intent_analysis.md
```

**功能**:
- 分析關鍵字意圖
- 檢查文章是否包含必要元素
- 計算匹配分數
- 提供優化建議

### 4. JSON 格式輸出

```bash
python .claude/skills/seo/intent_analyzer.py analyze "量化交易策略" --json
```

**用途**: 適合程式化處理或整合到其他工具

## 📊 輸出報告範例

```markdown
# SEO 搜尋意圖分析報告

**分析時間**: 2025-10-24T15:30:00

---

## 🎯 主要關鍵字

**關鍵字**: 如何使用 Claude Code

## 📊 意圖分類

- **主要意圖**: 教學型 (tutorial)
- **信心度**: 95%

## 💡 推薦文章結構

### 標題格式
```
【完整指南】如何{action} - {benefit}
```

**範例標題**:
- 【完整指南】如何使用 Claude Code 建立 AI Agent - 10分鐘快速上手

### 必須包含的元素
- 前置準備清單（環境、工具、知識要求）
- 逐步操作步驟（帶編號、帶截圖）
- 常見錯誤和解決方法
- 進階技巧和最佳實踐

### 建議結構
```markdown
## 引言（為什麼需要學這個）
## 前置準備
## 步驟一：...
## 步驟二：...
## 常見問題
## 總結與下一步
```

## 📈 當前文章匹配度

**匹配分數**: 0.85 / 1.0

```
████████████████████░ 85%
```

✅ **已包含的元素**:
- 逐步操作步驟（帶編號、帶截圖）
- 完整的可執行範例

⚠️ **缺少的元素**:
- 前置準備清單（環境、工具、知識要求）
- 常見錯誤和解決方法

## 🔧 優化建議

⚠️ **文章基本符合搜尋意圖，但有改進空間**

建議補充:
1. 增加「前置準備清單」相關內容
2. 增加「常見錯誤和解決方法」相關內容
```

## 🛠️ 在 Agent 中使用

### SEO Optimizer Agent 整合方式

在 `.claude/agents/seo-optimizer.md` 中加入以下步驟:

```markdown
### Step 0: 搜尋意圖分析

**目的**: 確保文章結構符合使用者的搜尋意圖

1. **提取主要關鍵字**
   - 從 `research_report.md` 取得目標關鍵字
   - 或從文章標題提取核心關鍵字

2. **執行意圖分析**
   ```bash
   python .claude/skills/seo/intent_analyzer.py analyze "{關鍵字}" \
     --article output/session_{timestamp}/final_article.md \
     --output output/session_{timestamp}/intent_analysis.md
   ```

3. **讀取分析結果**
   ```
   Read: output/session_{timestamp}/intent_analysis.md
   提取:
   - 主要意圖類型
   - 匹配分數
   - 缺少的元素
   ```

4. **決策邏輯**
   ```
   if 匹配分數 >= 0.85:
       → 文章符合意圖，繼續 SEO 優化
   elif 匹配分數 >= 0.7:
       → 記錄建議，進行微調優化
   else:
       → 提醒需要重大調整，考慮重構文章結構
   ```

5. **應用優化建議**
   - 根據缺少的元素調整 Meta Description
   - 優化標題以符合意圖類型
   - 在 SEO 報告中記錄意圖分析結果
```

### 使用範例

```python
# 在 Python 腳本中使用
from intent_analyzer import IntentAnalyzer

analyzer = IntentAnalyzer()

# 分析關鍵字
result = analyzer.analyze("如何使用 Claude Code")

print(f"意圖: {result['intent_label']}")
print(f"信心度: {result['confidence']}")

# 產生報告
report = analyzer.generate_report(result, "output/intent_report.md")
```

## ⚙️ 配置自訂規則

編輯 `.claude/skills/seo/intent_rules.yaml` 可以：

1. **調整關鍵字權重**
   ```yaml
   intent_patterns:
     tutorial:
       weight: 1.0  # 調整權重
   ```

2. **新增關鍵字**
   ```yaml
   tutorial:
     keywords:
       - 如何
       - 攻略  # 新增
       - 懶人包  # 新增
   ```

3. **設定閾值**
   ```yaml
   thresholds:
     min_confidence: 0.3
     match_threshold: 0.7
   ```

4. **產業特定規則**
   ```yaml
   industry_specific:
     your_industry:
       enabled: true
       boost_keywords:
         - 特定術語1
         - 特定術語2
       boost_factor: 1.1
   ```

## 📈 預期效果

根據優化方案預估，實施搜尋意圖分析後:

| 指標 | 預期改善 |
|------|---------|
| Google 點擊率 (CTR) | +15% ~ +25% |
| 搜尋排名 | 提升 2-5 名 |
| 頁面停留時間 | +10% ~ +20% |
| 跳出率 | -10% ~ -15% |

**原因**: 文章更精準地回答使用者的搜尋需求

## 🐛 疑難排解

### 問題 1: 找不到 intent_rules.yaml

**解決方案**: 工具會自動使用內建規則，或從範本複製一份配置檔

### 問題 2: 匹配分數異常低

**可能原因**:
- 關鍵字與文章主題不符
- 文章結構不完整
- 意圖類型判斷錯誤

**解決方案**:
1. 檢查關鍵字是否正確
2. 手動檢視文章是否包含必要元素
3. 參考報告中的「缺少元素」補充內容

### 問題 3: 意圖識別錯誤

**解決方案**:
1. 在 `intent_rules.yaml` 中調整關鍵字權重
2. 提供更明確的關鍵字
3. 手動指定意圖類型（進階功能）

## 📝 版本歷史

### v1.0.0 (2025-10-24)
- 初始版本發布
- 支援 6 種搜尋意圖識別
- 提供結構化建議和匹配度分析
- 可配置的規則系統

## 🔗 相關資源

- [Google 搜尋意圖指南](https://developers.google.com/search/docs/fundamentals/creating-helpful-content)
- [SEO 最佳實踐](https://developers.google.com/search/docs/fundamentals/seo-starter-guide)
- 系統優化方案文檔: `系統深度分析與優化方案_內容教練點評.md`

## 👥 貢獻

如發現問題或有改進建議，請更新 `intent_rules.yaml` 或提出反饋。

---

**維護者**: 喵哩文創 AI 寫手系統團隊
**最後更新**: 2025-10-24
